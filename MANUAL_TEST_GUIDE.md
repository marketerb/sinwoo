# 신우아이앤씨 수동 테스트 가이드

**목적:** QA 체크리스트의 실제 수행 방법 및 상세 지침  
**대상:** QA 테스터, 개발자  
**작성일:** 2026-06-01

---

## 📱 테스트 환경 준비

### 개발 환경 실행

```bash
# 1. 프로젝트 디렉토리로 이동
cd c:\laragon\www\sinwoo

# 2. 개발 서버 시작
npm run dev

# 3. 브라우저에서 접속
# http://localhost:3000
```

### 개발자 도구 열기

- **Windows/Linux:** `F12` 또는 `Ctrl+Shift+I`
- **macOS:** `Cmd+Option+I`

### 모바일 뷰포트 에뮬레이션

1. 개발자 도구 열기 (`F12`)
2. **Toggle device toolbar** (`Ctrl+Shift+M`)
3. 해상도 선택:
   - iPhone SE (375px): `375 x 667`
   - iPhone 12 (390px): `390 x 844`
   - iPad (768px): `768 x 1024`
   - 데스크톱 (1440px): `1440 x 900`

---

## 🔴 CRITICAL 테스트 프로토콜

### Test #1: 로그인 페이지 접근성

**목표:** 미인증 사용자가 /login에 접근 가능하고, 인증된 사용자는 자동으로 대시보드로 리다이렉트되는지 확인

**테스트 단계:**

```
1️⃣ 쿠키 초기화
   - DevTools > Application > Cookies > 모두 삭제
   - 또는 새로운 시크릿 창 열기 (Ctrl+Shift+N)

2️⃣ /login 접근 (비인증)
   - URL: http://localhost:3000/login
   - 예상 결과: ✅ 로그인 페이지 표시
   - 체크:
     □ 로그인 폼 렌더링됨
     □ ID 입력 필드 존재
     □ 비밀번호 입력 필드 존재
     □ 로그인 버튼 존재
     □ 신우아이앤씨 로고 표시됨

3️⃣ 로그인 (유효 자격증명)
   - ID: sinwoo
   - PW: 0909@@
   - 예상 결과: ✅ /dashboard로 리다이렉트
   - 체크:
     □ 즉시 리다이렉트 (307 또는 302)
     □ DevTools > Network에서 Location 헤더 확인
     □ auth-token 쿠키 생성 확인

4️⃣ /login 재접근 (인증됨)
   - URL: http://localhost:3000/login
   - 예상 결과: ✅ /dashboard로 자동 리다이렉트
   - 체크:
     □ 로그인 폼 표시 안 됨
     □ 즉시 대시보드로 이동
     □ 리다이렉트 확인 (DevTools > Network)
```

**통과 기준:**
- ✅ 모든 단계 완료

**실패 시 확인:**
- middleware.ts에서 `/login` 경로 matcher 포함 확인
- 토큰 검증 로직 확인

---

### Test #2: JWT 토큰 검증

**목표:** JWT 토큰이 올바르게 생성, 저장, 검증되는지 확인

**테스트 단계:**

```
1️⃣ 토큰 생성 확인
   - DevTools > Network 탭 열기
   - /login 페이지 접속 (비인증)
   - 자격증명 입력: sinwoo / 0909@@
   - 로그인 버튼 클릭
   - Network 탭에서 POST /api/login 요청 확인
   - 예상 결과:
     □ 상태: 200 OK
     □ Response Body에 token 필드 존재 (또는 쿠키로 설정)
     □ Set-Cookie 헤더에 auth-token 존재

2️⃣ 토큰 저장 확인
   - DevTools > Application > Cookies
   - auth-token 쿠키 확인
   - 세부사항:
     □ Name: auth-token
     □ Value: JWT 토큰 (eyJ... 형식)
     □ Expires: 24시간 이내
     □ HttpOnly: 체크됨 ✓
     □ Secure: 체크됨 ✓ (프로덕션)
     □ SameSite: Lax 또는 Strict

3️⃣ 토큰 유효성 검증
   - /dashboard 페이지 표시됨 (유효한 토큰)
   - 예상 결과:
     □ 대시보드 콘텐츠 로드됨
     □ 포트폴리오 관리, 연혁 관리 등 메뉴 표시

4️⃣ 토큰 만료 시뮬레이션 (선택사항)
   - DevTools > Application에서 auth-token 쿠키 만료일 변경
   - 또는 쿠키 값을 invalid로 변경
   - 페이지 새로고침 (F5)
   - 예상 결과: ✅ /login으로 리다이렉트
```

**통과 기준:**
- ✅ JWT 토큰 생성, 저장, 검증 모두 완료

**실패 시 확인:**
- `/api/login` 엔드포인트 확인
- jose 라이브러리 토큰 생성 로직 확인
- middleware.ts 토큰 검증 로직 확인

---

### Test #3: 대시보드 보호

**목표:** 인증되지 않은 사용자가 대시보드에 접근할 수 없는지 확인

**테스트 단계:**

```
1️⃣ 쿠키 초기화
   - DevTools > Application > Cookies > auth-token 삭제
   - 또는 시크릿 창 사용

2️⃣ /dashboard 접근 (비인증)
   - URL: http://localhost:3000/dashboard
   - 예상 결과: ✅ /login으로 자동 리다이렉트
   - 확인:
     □ 대시보드 콘텐츠 표시 안 됨
     □ 즉시 /login으로 이동
     □ Network 탭에서 307/302 리다이렉트 확인

3️⃣ /dashboard/portfolio 접근 (비인증)
   - URL: http://localhost:3000/dashboard/portfolio
   - 예상 결과: ✅ /login으로 자동 리다이렉트
   - 확인:
     □ 포트폴리오 관리 페이지 표시 안 됨
     □ /login으로 이동

4️⃣ /dashboard/history 접근 (비인증)
   - URL: http://localhost:3000/dashboard/history
   - 예상 결과: ✅ /login으로 자동 리다이렉트

5️⃣ /dashboard/company 접근 (비인증)
   - URL: http://localhost:3000/dashboard/company
   - 예상 결과: ✅ /login으로 자동 리다이렉트
```

**통과 기준:**
- ✅ 모든 대시보드 경로에서 /login으로 리다이렉트

**실패 시 확인:**
- middleware.ts matcher 설정 확인: `["/dashboard/:path*", "/login"]`
- 토큰 검증 로직 확인

---

### Test #4: 환경 변수 및 API 키 검증

**목표:** 민감한 정보가 노출되지 않았는지 확인

**테스트 단계:**

```
1️⃣ .env.local 파일 확인
   - 프로젝트 루트에 .env.local 존재
   - 내용:
     □ NEXT_PUBLIC_SUPABASE_URL=https://...
     □ NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
   - .gitignore에 포함되어 있는지 확인

2️⃣ 소스 코드 확인
   - app/page.tsx, app/layout.tsx 등 열어서 검토
   - 확인 사항:
     □ API 키가 하드코딩되지 않음
     □ 민감한 정보(비밀번호, 토큰)가 코드에 없음
     □ process.env.NEXT_PUBLIC_*만 사용

3️⃣ 프로덕션 환경 변수
   - Vercel 또는 호스팅 서비스에서 환경 변수 설정 확인
   - NEXT_PUBLIC_SUPABASE_URL: 설정됨
   - NEXT_PUBLIC_SUPABASE_ANON_KEY: 설정됨
   - (서비스 롤 키는 프로덕션에 불필요)

4️⃣ 네트워크 요청 검사
   - DevTools > Network 탭 열기
   - API 요청들 확인:
     □ /api/login - 응답에 토큰이나 민감한 정보 노출 여부
     □ /api/logout - 쿠키 삭제 확인
     □ /api/portfolios - 데이터만 반환 (키 미포함)
   - 확인 사항:
     □ URL에 API 키 포함 안 됨
     □ 요청 헤더에 Authorization 사용 가능

5️⃣ 콘솔 로그 확인
   - DevTools > Console 탭 열기
   - 콘솔에서 확인:
     □ API 키가 로깅되지 않음
     □ 민감한 정보가 노출되지 않음
     □ 디버그 메시지 없음 (프로덕션)
```

**통과 기준:**
- ✅ 환경 변수 설정됨
- ✅ 소스 코드에 API 키 미포함
- ✅ 콘솔에 민감한 정보 미노출

---

## 🟠 HIGH 우선순위 테스트

### Test #5: 네비게이션 앵커링크

**목표:** 모든 앵커링크가 올바른 섹션으로 스크롤되는지 확인

**테스트 단계:**

```
🖥️  데스크톱 (1440px)

1️⃣ HOME 앵커링크
   - 네비게이션에서 "HOME" 클릭
   - 예상: 페이지 맨 위로 스크롤 (Hero 섹션)
   - URL: http://localhost:3000/#home
   - 확인:
     □ 스크롤 애니메이션 부드러움
     □ Hero 배경이미지 보임
     □ CND 로고와 배경 그라데이션 표시

2️⃣ COMPANY 앵커링크
   - 네비게이션에서 "COMPANY" 클릭
   - 예상: About 섹션으로 스크롤
   - URL: http://localhost:3000/#about
   - 확인:
     □ 회사정보 섹션으로 이동
     □ 텍스트, 이미지 등 콘텐츠 표시

3️⃣ BUSINESS 앵커링크
   - 네비게이션에서 "BUSINESS" 클릭
   - 예상: Business 섹션으로 스크롤
   - URL: http://localhost:3000/#business
   - 확인:
     □ 사업영역 섹션으로 이동
     □ 4가지 사업 카드 표시

4️⃣ PORTFOLIO 앵커링크
   - 네비게이션에서 "PORTFOLIO" 클릭
   - 예상: Portfolio 섹션으로 스크롤
   - URL: http://localhost:3000/#portfolio
   - 확인:
     □ 포트폴리오 섹션으로 이동
     □ 프로젝트 그리드 표시

5️⃣ CONTACT 앵커링크
   - 네비게이션에서 "CONTACT" 클릭
   - 예상: Contact 섹션으로 스크롤
   - URL: http://localhost:3000/#contact
   - 확인:
     □ 문의 섹션으로 이동
     □ 문의 폼 표시

📱 모바일 (375px)

1️⃣ 햄버거 메뉴 열기
   - 네비게이션 우측의 "☰" 아이콘 클릭
   - 예상: 메뉴 펼침
   - 확인:
     □ 메뉴 아이콘 표시됨
     □ 클릭 시 메뉴 오버레이 나타남

2️⃣ 메뉴 항목 클릭
   - 메뉴에서 "HOME" 클릭
   - 예상: 해당 섹션으로 스크롤 + 메뉴 자동 닫힘
   - 확인:
     □ 모든 메뉴 항목에 대해 동일 동작
     □ 메뉴 외부 클릭 시도 닫힘 (필요한 경우)

3️⃣ 메뉴 닫기
   - 메뉴 오픈 상태에서 X 아이콘 클릭
   - 또는 메뉴 항목 클릭
   - 예상: 메뉴 닫힘
   - 확인:
     □ 메뉴 오버레이 사라짐
     □ 배경 스크롤 가능
```

**통과 기준:**
- ✅ 모든 앵커링크가 올바른 섹션으로 이동
- ✅ 스크롤 애니메이션 부드러움
- ✅ 모바일 메뉴 정상 작동

**실패 시 확인:**
- Navigation.tsx에서 앵커링크 href 확인
- 각 섹션의 id 속성 확인 (id="home", id="about" 등)

---

### Test #6: 로그인 폼 유효성 검증

**목표:** 로그인 폼이 올바르게 입력을 검증하는지 확인

**테스트 단계:**

```
1️⃣ 유효한 자격증명 로그인
   - URL: http://localhost:3000/login
   - ID: sinwoo
   - PW: 0909@@
   - 로그인 버튼 클릭
   - 예상 결과: ✅ /dashboard로 리다이렉트
   - 확인:
     □ "로그인 중..." 텍스트 표시 중 로딩 상태
     □ 리다이렉트 후 대시보드 표시
     □ 에러 메시지 없음

2️⃣ 잘못된 ID
   - ID: wrong_id
   - PW: 0909@@
   - 로그인 버튼 클릭
   - 예상 결과: ✅ 에러 메시지 표시
   - 확인:
     □ 에러 메시지: "ID 또는 비밀번호가 틀렸습니다"
     □ 빨간색 배경의 알림창 표시
     □ /login 페이지 유지 (리다이렉트 안 됨)
     □ ID와 PW 필드 데이터 유지 (재입력 가능)

3️⃣ 잘못된 비밀번호
   - ID: sinwoo
   - PW: wrong_password
   - 로그인 버튼 클릭
   - 예상 결과: ✅ 에러 메시지 표시
   - 확인:
     □ 동일한 에러 메시지 표시
     □ /login 페이지 유지

4️⃣ 빈 필드 제출
   - ID: (빈 칸)
   - PW: (빈 칸)
   - 로그인 버튼 클릭
   - 예상 결과: 브라우저 기본 검증 또는 앱 검증
   - 확인:
     □ 필수 필드 경고 표시
     □ 또는 양쪽 필드 모두 입력 필요 메시지

5️⃣ 로딩 상태 확인
   - 로그인 버튼 클릭 후 즉시 다시 클릭 시도
   - 예상 결과: 버튼 비활성화 또는 로딩 상태
   - 확인:
     □ 중복 요청 방지 (버튼 disabled)
     □ "로그인 중..." 텍스트 표시
     □ 오직 한 번의 /api/login 요청 발생

6️⃣ 네트워크 에러 시뮬레이션
   - DevTools > Network 탭에서 Throttling: Offline 설정
   - 로그인 시도
   - 예상 결과: ✅ 에러 메시지 표시
   - 확인:
     □ "로그인 중 오류가 발생했습니다" 메시지
     □ /login 페이지 유지
```

**통과 기준:**
- ✅ 유효한 자격증명으로 로그인 성공
- ✅ 잘못된 자격증명으로 에러 메시지 표시
- ✅ 네트워크 에러 처리

---

### Test #7: 포트폴리오 데이터 로드

**목표:** Supabase에서 포트폴리오 데이터가 올바르게 로드되는지 확인

**테스트 단계:**

```
1️⃣ 페이지 로드 성능
   - 홈페이지(/): http://localhost:3000/
   - DevTools > Network 탭
   - 예상 결과: 데이터 로드됨
   - 확인:
     □ GET /api/portfolios 요청 발생
     □ 응답: 200 OK
     □ 응답 시간: 1초 이내 (목표)

2️⃣ 포트폴리오 데이터 구조
   - GET /api/portfolios 응답 확인
   - 예상 데이터 구조:
     ```json
     [
       {
         "id": "...",
         "title": "보라매 파르크힐",
         "location": "서울",
         "status": "completed",
         "image_url": "...",
         ...
       }
     ]
     ```
   - 확인:
     □ 배열 형식 반환
     □ 각 항목에 id, title, location 필드
     □ 이미지 URL 유효함

3️⃣ 포트폴리오 렌더링 (홈페이지)
   - 홈페이지 포트폴리오 섹션 표시
   - 예상 결과:
     □ 6개 프로젝트 카드 표시 (또는 데이터베이스 기준)
     □ 각 카드에 프로젝트명 표시
     □ 각 카드에 위치 정보 표시
     □ 각 카드에 이미지 표시 (또는 플레이스홀더)

4️⃣ 포트폴리오 섹션 스크롤
   - 홈페이지에서 #portfolio로 스크롤
   - 예상 결과:
     □ 포트폴리오 섹션으로 이동
     □ 프로젝트 그리드 표시
     □ 모든 데이터 로드 완료

5️⃣ 포트폴리오 대시보드
   - 로그인 후 /dashboard/portfolio 접속
   - 예상 결과:
     □ 포트폴리오 관리 인터페이스 표시
     □ 프로젝트 목록 표시
     □ 추가/수정/삭제 버튼 표시 (구현된 경우)

6️⃣ 이미지 로드 확인
   - DevTools > Network > Img 필터
   - 예상 결과:
     □ 이미지 요청 발생
     □ 이미지 크기 확인 (최적화 필요 시 표시)
     □ 로드 시간 확인 (1초 이내 목표)

7️⃣ 에러 핸들링
   - DevTools > Network에서 Offline 설정
   - 페이지 새로고침
   - 예상 결과:
     □ 에러 메시지 표시 또는 폴백 UI
     □ 페이지 완전히 깨지지 않음
     □ 사용자 경험 영향 최소화
```

**통과 기준:**
- ✅ 포트폴리오 데이터가 로드됨
- ✅ 모든 프로젝트가 올바르게 표시됨
- ✅ 이미지가 정상적으로 로드됨
- ✅ 네트워크 에러 처리됨

---

## 🟡 MEDIUM 우선순위 테스트

### Test #8: 반응형 디자인

**목표:** 다양한 해상도에서 레이아웃이 올바르게 표시되는지 확인

**테스트 해상도:**

```
모바일:
- 320px (iPhone SE)
- 375px (iPhone 12)
- 414px (iPhone 14 Plus)

태블릿:
- 768px (iPad)
- 1024px (iPad Pro)

데스크톱:
- 1440px (일반)
- 1920px (대형)
```

**테스트 프로토콜:**

```
각 해상도마다:

1️⃣ 페이지 로드
   - URL: http://localhost:3000/
   - DevTools > Toggle device toolbar (Ctrl+Shift+M)
   - 해상도 설정
   - 예상: 페이지가 해상도에 맞게 렌더링됨

2️⃣ 네비게이션 검사
   - 네비게이션 표시 확인
   - 메뉴 아이템 배치 확인
   - 모바일(<768px)에서 햄버거 메뉴 표시 확인
   - 데스크톱(≥768px)에서 수평 메뉴 표시 확인

3️⃣ 콘텐츠 레이아웃
   - Hero 섹션: 배경 이미지가 화면을 채우는지 확인
   - 카드 그리드: 
     * 모바일: 1열
     * 태블릿: 2열
     * 데스크톱: 3열 (또는 설계 기준)
   - 폰트 크기: 너무 작거나 크지 않은지 확인

4️⃣ 이미지 최적화
   - 이미지가 화면에 맞게 크기 조정됨
   - 가로 스크롤 없음
   - 로드 시간 적절함

5️⃣ 버튼 및 입력 필드
   - 터치 크기: 최소 44x44px
   - 클릭 가능한 영역 충분함
   - 입력 필드가 명확함

6️⃣ 텍스트 가독성
   - 폰트 크기: 최소 14px
   - 라인 높이: 적절함 (1.5배 이상)
   - 색상 대비: 명확함
   - 모바일에서 텍스트가 자르히지 않음

7️⃣ 스크롤 동작
   - 스크롤이 부드러움 (끊김 없음)
   - 가로 스크롤 없음
   - 스크롤 진행도 바 정상 작동
```

**검사 체크리스트:**

```
해상도: ___________

☐ 헤더 네비게이션
  ☐ 로고 표시됨
  ☐ 메뉴 항목 모두 표시됨
  ☐ 메뉴 자르히지 않음
  ☐ 클릭 가능한 영역 충분함

☐ Hero 섹션
  ☐ 배경 이미지 로드됨
  ☐ 텍스트 읽기 가능함
  ☐ 버튼 클릭 가능함
  ☐ 가로 스크롤 없음

☐ 콘텐츠 섹션
  ☐ 카드 그리드 레이아웃 정상
  ☐ 간격(margin/padding) 적절함
  ☐ 텍스트 잘리지 않음
  ☐ 이미지 비율 유지됨

☐ 푸터
  ☐ 연락처 정보 표시됨
  ☐ 링크 클릭 가능함
  ☐ 자르히지 않음

☐ 폼
  ☐ 입력 필드 보임
  ☐ 라벨 명확함
  ☐ 버튼 클릭 가능함
  ☐ 입력 에러 메시지 표시됨

☐ 전반적 사용성
  ☐ 가로 스크롤 없음
  ☐ 텍스트 읽기 가능함
  ☐ 버튼 터치 크기 적절 (모바일)
  ☐ 애니메이션 부드러움
```

---

### Test #9: 성능 측정

**목표:** 페이지 로딩 속도 및 성능 지표를 측정

**도구: Chrome DevTools Lighthouse**

```
1️⃣ Lighthouse 보고서 생성
   - DevTools > Lighthouse 탭
   - 분석 대상: 모바일 및 데스크톱
   - 생성

2️⃣ 성능 지표 확인
   Performance Score (목표: 90+):
   - 현재: ___
   - 통과: ☐ (90 이상)

   First Contentful Paint (FCP) (목표: < 1.8s):
   - 현재: ___
   - 통과: ☐

   Largest Contentful Paint (LCP) (목표: < 2.5s):
   - 현재: ___
   - 통과: ☐

   Cumulative Layout Shift (CLS) (목표: < 0.1):
   - 현재: ___
   - 통과: ☐

   Speed Index (목표: < 3.4s):
   - 현재: ___
   - 통과: ☐

3️⃣ 최적화 권장사항 검토
   - 이미지 최적화
   - 미사용 CSS/JS 제거
   - 캐싱 전략
   - 포기하지 말고 우선순위대로 수정
```

**도구: Chrome DevTools Network**

```
1️⃣ 네트워크 성능 측정
   - DevTools > Network 탭 열기
   - 캐시 비활성화 (체크박스)
   - 느린 네트워크 시뮬레이션: "Slow 3G" 선택
   - 페이지 새로고침 (Ctrl+Shift+R)

2️⃣ 로딩 시간 측정
   - Finish (전체 로딩 시간): ___
   - 목표: 5초 이내
   - 통과: ☐

3️⃣ 리소스별 크기 분석
   - 이미지 크기 합계: ___
   - JavaScript 크기 합계: ___
   - CSS 크기 합계: ___

4️⃣ 느린 리소스 식별
   - 로딩 시간 1초 이상인 리소스 목록
   - 원인 분석 및 최적화 필요
```

---

### Test #10: 문의 폼 제출

**목표:** 문의 폼이 올바르게 작동하고 데이터를 처리하는지 확인

**테스트 단계:**

```
1️⃣ 폼 필드 확인
   - 홈페이지에서 #contact로 스크롤
   - 문의 폼 표시 확인:
     ☐ 이름 (text 입력)
     ☐ 이메일 (email 입력)
     ☐ 전화 (tel 입력)
     ☐ 제목 (text 입력)
     ☐ 메시지 (textarea 입력)
     ☐ 제출 버튼

2️⃣ 유효한 데이터 제출
   - 모든 필드 입력:
     * 이름: 테스트 이름
     * 이메일: test@example.com
     * 전화: 01012345678
     * 제목: 테스트 제목
     * 메시지: 테스트 메시지
   - 제출 버튼 클릭
   - DevTools > Network 탭에서 POST /api/contact 확인
   - 예상 결과:
     ☐ 상태: 200 OK
     ☐ 성공 메시지 표시: "문의가 접수되었습니다"
     ☐ 폼이 리셋됨 (필드 초기화)

3️⃣ 이메일 검증
   - 잘못된 이메일: test@invalid
   - 제출 시도
   - 예상 결과:
     ☐ 브라우저 기본 검증: "올바른 이메일을 입력하세요"
     ☐ 또는 앱 검증: 에러 메시지 표시

4️⃣ 필수 필드 검증
   - 이름만 입력, 나머지 비움
   - 제출 시도
   - 예상 결과:
     ☐ 필수 필드 경고: "필수 항목입니다"
     ☐ 빈 필드에 포커스 이동

5️⃣ 전화번호 검증
   - 전화: abc123 (숫자 아님)
   - 제출 시도
   - 예상 결과:
     ☐ 검증 오류 메시지
     ☐ 또는 앱 검증: "숫자만 입력하세요"

6️⃣ 네트워크 에러 시뮬레이션
   - DevTools > Network > Offline
   - 폼 입력 후 제출 시도
   - 예상 결과:
     ☐ 에러 메시지: "전송 중 오류가 발생했습니다"
     ☐ 폼 데이터 유지 (재시도 가능)
   - Network을 Online으로 복구
   - 재시도

7️⃣ 데이터 처리 확인
   - 성공 제출 후 백엔드 확인 (선택사항):
     ☐ Supabase에 데이터 저장됨
     ☐ 관리자 이메일로 알림 전송됨 (구현된 경우)
```

**통과 기준:**
- ✅ 유효한 데이터 제출 성공
- ✅ 검증 오류 메시지 표시
- ✅ 네트워크 에러 처리

---

## 🟢 LOW 우선순위 테스트

### Test #11: 접근성 (WCAG 2.1 AA)

**도구: axe DevTools 또는 WAVE**

```
1️⃣ axe DevTools 설치 및 실행
   - Chrome 웹스토어에서 "axe DevTools" 설치
   - DevTools > axe DevTools 탭
   - "Scan ALL of my page" 클릭

2️⃣ 접근성 이슈 검토
   - Violations (필수 수정)
   - Best practices (권장 수정)
   - 각 이슈의 심각도 확인

3️⃣ 색상 대비 검사
   - 모든 텍스트의 대비율 확인:
     * 일반 텍스트: 4.5:1 이상
     * 큰 텍스트 (18pt+): 3:1 이상
     * 버튼/링크: 4.5:1 이상

4️⃣ 키보드 네비게이션
   - 마우스 사용 금지
   - Tab 키로 모든 인터랙티브 요소 접근 가능:
     ☐ 링크 (href)
     ☐ 버튼
     ☐ 입력 필드
     ☐ 메뉴
   - Shift+Tab으로 역방향 이동
   - Enter로 링크/버튼 활성화
   - Space로 체크박스 활성화

5️⃣ 스크린 리더 호환성 (NVDA 또는 JAWS)
   - 페이지 제목 읽음
   - 헤더(H1-H6) 구조 확인
   - 이미지 alt 텍스트 읽음
   - 폼 라벨 읽음
   - 버튼 텍스트 읽음

6️⃣ 폰트 크기 가독성
   - 모든 텍스트: 최소 14px
   - 라인 높이: 최소 1.5배
   - 글자 간격: 최소 0.12em
   - 단어 간격: 최소 0.16em
```

---

### Test #12: 브라우저 호환성

**테스트 브라우저:**

```
데스크톱:
- Chrome (최신 2개 버전)
- Firefox (최신 2개 버전)
- Safari (최신 2개 버전)
- Edge (최신 2개 버전)

모바일:
- Chrome (Android)
- Safari (iOS)
```

**테스트 프로토콜 (각 브라우저):**

```
1️⃣ 페이지 로드
   - 홈페이지 로드
   - 예상: 렌더링 오류 없음
   - 확인: ☐

2️⃣ 네비게이션
   - 앵커링크 클릭
   - 예상: 올바른 섹션으로 스크롤
   - 확인: ☐

3️⃣ 폼 입력
   - 로그인 또는 문의 폼 입력
   - 예상: 입력 필드 작동
   - 확인: ☐

4️⃣ 이미지 로드
   - 모든 이미지 표시
   - 예상: 이미지 렌더링 정상
   - 확인: ☐

5️⃣ 애니메이션
   - 스크롤 진행도 바
   - 호버 이펙트
   - 페이드 애니메이션
   - 예상: 부드러운 애니메이션
   - 확인: ☐

6️⃣ 콘솔 에러
   - DevTools > Console
   - 예상: 에러 없음 (경고는 가능)
   - 확인: ☐
```

---

## 📋 최종 체크리스트

테스트 완료 후 다음을 확인하세요:

```
CRITICAL 항목 (모두 필수):
☐ 로그인 페이지 접근성
☐ JWT 토큰 검증
☐ 대시보드 보호
☐ 환경 변수 보안

HIGH 항목 (배포 전 완료):
☐ 네비게이션 앵커링크
☐ 로그인 폼 검증
☐ 포트폴리오 데이터 로드

MEDIUM 항목 (배포 후 1주일 내):
☐ 반응형 디자인 (3개 브레이크포인트)
☐ 성능 측정 (Lighthouse 점수)
☐ 문의 폼 제출

LOW 항목 (여유 시간):
☐ 접근성 검사 (axe DevTools)
☐ 브라우저 호환성 (3개 브라우저)
```

---

## 🐛 버그 발견 시 보고

버그 발견 시 다음 정보를 기록하세요:

```
버그 #: ___
제목: _____________________________

환경:
- 브라우저: _______
- OS: _______
- 해상도: _______
- URL: _______

재현 단계:
1. _______
2. _______
3. _______

예상 동작:
_______

실제 동작:
_______

스크린샷/비디오:
[첨부]

콘솔 에러:
[복사/붙여넣기]
```

---

## 📞 문의

테스트 중 질문이 있으면:

- **개발팀:** izer84@gmail.com
- **프로젝트 담당자:** sinwooinc2014@naver.com

---

**마지막 업데이트:** 2026-06-01
